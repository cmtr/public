<section id="team" class="team section-bg">
	<div class="container" data-aos="fade-up">

		<div class="section-title">
			<h2><%= team.title %></h2>
			<p><%= team.paragraph %></p>
		</div>

		<div class="row">

			<% team.team.forEach((item) => { %>
				<div class="col-lg-6 col-md-6">
					<%- include('./team-member-card', { item })%>
				</div>
			<% }) %>

		</div>

	</div>
</section>